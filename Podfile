# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LabTests' do

pod 'iRate' # A handy class that prompts users of your iPhone or Mac App Store app to rate your application after using it for a while.
pod 'Firebase' # Firebase for iOS
pod 'Firebase/Database' # Firebase database

end
